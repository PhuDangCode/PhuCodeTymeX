pluginManagement {
    repositories {
        google()
        jcenter()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        jcenter()
    }
}

rootProject.name = "CurrencyConverter"
include(":app")
